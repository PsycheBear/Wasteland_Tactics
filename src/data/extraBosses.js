// === EXTRA BOSSES (overhaul) ===
//
// Mothman and the older alt-bosses were retired in the character overhaul.
// This file remains so bosses.js can still `import { extraBosses }` from it
// without an exception — the array is intentionally empty.
//
// Future content can push new boss objects into this array using the same
// schema as BOSS_DATABASE entries (round + mechanic + stats + iconImg).

export const extraBosses = [];
